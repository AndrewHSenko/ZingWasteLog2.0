{
  "name": "zingwastelog",
  "private": true,
  "description": "Express API for the Zing waste log. Runs as a Vercel serverless function in production (see api/) and as a normal server locally (backend/server.js).",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "http-status-codes": "^2.3.0",
    "mongoose": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  },
  "scripts": {
    "dev": "nodemon backend/server.js",
    "start": "node backend/server.js"
  }
}
